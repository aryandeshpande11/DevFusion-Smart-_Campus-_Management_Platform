# Base URL of the Express API (see server/ from the system design doc)
VITE_API_URL=http://localhost:5000/api

# Google OAuth client id used by the "Continue with Google" button
VITE_GOOGLE_CLIENT_ID=
